# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
*.egg
.eggs/

# Virtual environments
.venv/
venv/
env/

# Environment variables — never commit secrets
.env
.env.*
!.env.example

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Testing
.pytest_cache/
.coverage
htmlcov/

# Node modules (legacy, no longer used)
node_modules/

# Local notes / outreach drafts — never push
LARRY-STEPS.txt
REACOUT.txt
REACHOUT.txt
nextsteps.txt
*.local.txt
plans/
"plans folder/"

# Local sample CSVs used for manual upload testing — not part of the repo.
# Anything the user drops in to try the /api/upload path lives here
# transiently; don't leak it into git.
MOCK.csv
*.sample.csv
